# aBaiAutoplus 开发模式启动说明

## 📋 一键启动脚本说明

项目已配置好开发模式启动脚本，可以一键启动前端和后端开发服务器。

### 🚀 快速启动

双击运行以下任一文件：

```bash
dev-start.bat          # Windows 批处理文件（推荐）
dev-start.ps1          # PowerShell 脚本（高级用户）
```

**或者**在命令行中执行：

```bash
# 方式1：使用 bat 文件
dev-start.bat

# 方式2：直接运行 PowerShell 脚本
powershell -ExecutionPolicy Bypass -File dev-start.ps1
```

### ✅ 脚本功能

启动脚本会自动完成以下任务：

1. **检查端口占用**：确保 8000（后端）和 5173（前端）端口可用
2. **检查依赖**：
   - Python 虚拟环境（`.venv/`）
   - Python 依赖包（`requirements.txt`）
   - Playwright 浏览器驱动
   - 前端依赖（`frontend/node_modules`）
3. **自动安装缺失的依赖**
4. **同时启动前后端服务**：
   - 后端：FastAPI + uvicorn（支持热重载）
   - 前端：Vite 开发服务器（支持 HMR）
5. **自动打开浏览器**（可选）
6. **统一日志管理**

### 🎯 访问地址

启动成功后，在浏览器访问：

- **前端界面**：http://localhost:5173
- **后端 API**：http://localhost:8000
- **API 文档**：http://localhost:8000/docs

### 📝 脚本参数

```powershell
dev-start.ps1 [-SkipCheck] [-NoBrowser]

参数说明：
  -SkipCheck      跳过依赖检查（适合依赖已安装的情况，加快启动速度）
  -NoBrowser      不自动打开浏览器
```

示例：

```bash
# 跳过依赖检查，加快启动
dev-start.bat -SkipCheck

# 不打开浏览器
dev-start.bat -NoBrowser

# 组合使用
dev-start.bat -SkipCheck -NoBrowser
```

### 🛑 停止服务

按 `Ctrl + C` 停止所有服务，脚本会自动清理前后端进程。

### 📂 日志文件

日志文件保存在 `logs/` 目录：

- `logs/backend.out.log` - 后端标准输出
- `logs/backend.err.log` - 后端错误输出
- `logs/frontend.out.log` - 前端标准输出
- `logs/frontend.err.log` - 前端错误输出

### 🔧 故障排查

#### 1. 端口被占用

```bash
# 查看占用端口的进程
netstat -ano | findstr ":8000"
netstat -ano | findstr ":5173"

# 强制停止进程（替换 PID）
taskkill /F /PID <进程ID>
```

#### 2. PowerShell 执行策略错误

如果提示"无法加载文件，因为在此系统上禁止运行脚本"，执行：

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

#### 3. Python 虚拟环境问题

删除虚拟环境并重新创建：

```bash
rmdir /s /q .venv
python -m venv .venv
```

#### 4. 前端依赖问题

清理并重新安装：

```bash
cd frontend
rmdir /s /q node_modules
npm install
```

### ⚙️ 首次运行配置

首次运行时，脚本会自动：

1. 创建 Python 虚拟环境（约 1-2 分钟）
2. 安装 Python 依赖包（约 2-3 分钟）
3. 下载 Playwright 浏览器驱动（约 1-2 分钟，约 200MB）
4. 安装前端依赖（约 1-2 分钟）

**总计首次运行约需 5-10 分钟**，后续启动仅需 10-20 秒。

### 🔐 配置环境变量（可选）

如需自定义配置，复制 `.env.example` 为 `.env`：

```bash
copy .env.example .env
```

常用配置项：

```env
# 设置访问密码（公网部署必需）
APP_PASSWORD=your-password-here

# 数据库路径
ACCOUNT_MANAGER_DATABASE_URL=sqlite:///./data/account_manager.db

# 代理配置（如需访问外部服务）
HTTP_PROXY=http://127.0.0.1:7890
HTTPS_PROXY=http://127.0.0.1:7890
```

### 📊 验证服务状态

使用内置的健康检查脚本：

```bash
python scripts/smoke.py
```

该脚本会测试所有核心 API 端点。

## 🎓 开发模式 vs 生产模式

| 模式 | 前端 | 后端 | 用途 |
|------|------|------|------|
| **开发模式** | Vite Dev Server (5173) | uvicorn --reload (8000) | 本地开发、调试 |
| **生产模式** | 构建后的静态文件 | uvicorn (8000) | 生产部署 |

开发模式特点：
- ✅ 前端热更新（HMR）
- ✅ 后端代码自动重载
- ✅ 详细的调试信息
- ✅ Source Map 支持
- ⚠️ 不适合公网部署

## 📚 相关命令

```bash
# 仅启动后端（手动模式）
.venv\Scripts\activate
python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload

# 仅启动前端（手动模式）
cd frontend
npm run dev

# 构建前端生产版本
cd frontend
npm run build

# 运行测试
pytest

# 代码检查
cd frontend
npm run lint
```

---

**Author**: wangqiupei  
**Last Updated**: 2026-07-02
